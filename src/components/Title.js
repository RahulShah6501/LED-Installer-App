import React from 'react';

function Title() {
  return (
    <header>
      <h2>Welcome to LED Installation Designer</h2>
      <p>Create a custom LED installation diagram based on your project needs.</p>
    </header>
  );
}

export default Title; // Make sure it's a default export
